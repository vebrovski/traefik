# Project variables
PROJECT_NAME=project-name
PROJECT_HOSTNAME=project-hostname.localhost

# Database variables
APP_DB_HOST=mysql
APP_DB_NAME=dbname
APP_DB_USER=dbuser
APP_DB_PASS=dbpass
APP_DB_PORT=3306 # Change if already in use. Don't forget to change it in docker-compose.yml mysql service
APP_DB_PREFIX=
APP_DB_CHARSET=utf8mb4
APP_DB_COLLATION=utf8mb4_unicode_ci
